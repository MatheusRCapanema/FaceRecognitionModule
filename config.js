// config.js para o módulo de reconhecimento facial
const config = {
    dataPath: "modules/face_recognition/user_data",
    updateInterval: 2,
    // Outras configurações padrões
};

module.exports = config;
